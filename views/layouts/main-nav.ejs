<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <img src="./clovr_logo_dark_header_transparent.png" alt="" style="height: 80px;">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="/"><h6><i class="bi bi-house-fill"></i> Home</h6></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/keranjang"><h6><i class="bi bi-cart4"></i> Keranjang <p class="text-success" style="position: absolute;">
                <% if (keranjang.length) { %>
                    <%= keranjang.map(keranjang => keranjang.jumlah).reduce((prev, next) => prev + next) %> items
                <% } else {%>
                    0 items
                <% } %>
            </p></h6></a>
            </li>
          </ul>
          <form method="GET" action="/logout" class="d-flex">
            <button class="btn btn-outline-danger" type="submit" onclick="return confirm('Anda yakin?')">Logout</button>
          </form>
        </div>
      </div>
    </nav>